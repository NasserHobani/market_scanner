"""ماسح متعدد الأسواق — محرك مستقل تماماً عن أي واجهة ويب."""
__version__ = "0.1.0"

# ‏.env‎ يُحمَّل عند استيراد الحزمة لا عند إقلاع Django.
#
# كان القارئ داخل ``web/config/settings.py`` وحده، فكان السلوك
# يتفرّع بحسب كيف شُغّل الرمز: ``SAHMK_API_KEY`` يصل من اللوحة ولا
# يصل من أداة سطر الأوامر — فتقول الأداة «المفتاح غير مضبوط»
# والمفتاح في مكانه. انظر ``scanner/env.py``.
from . import env as _env  # noqa: F401,E402
