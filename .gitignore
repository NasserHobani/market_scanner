# أسرار — لا تُرفع أبداً
#
# ‏.env.bak* و‎.env.*‎ ليسا زيادة: نسخةٌ احتياطية من ‎.env‎ تحمل
# المفاتيح نفسها، و‎.env‎ وحدها لا تُطابقها. والاستثناء الوحيد
# ‎.env.example‎ — وهو قالب بلا قيم ويجب أن يُرفع.
.env
.env.*
!.env.example
!.env.docker.example
*.key

__pycache__/
*.py[cod]
.venv/
venv/

# بيانات ومخرجات
data/
reports/

# حزمة النقل إلى الخادم — نسخةُ القاعدة كاملةً وشموعُ الأسواق.
# ‏١٥٠ ميغابايت، وفيها كل صفقاتك وإعداداتك. تُبنى عند الحاجة
# بـ ``python tools_ship.py`` ولا مكان لها في مستودع.
ship/
staticfiles/
*.sqlite3

.pytest_cache/
.idea/
.vscode/
data/verdicts.jsonl
data/squeeze/
data/pes/
